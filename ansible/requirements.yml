---
# Ansible Galaxy requirements for SIB Fleet Management
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.5.0"
  
  - name: community.docker
    version: ">=3.0.0"
  
  - name: community.general
    version: ">=7.0.0"
