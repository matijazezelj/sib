# Ignore all certificate files by default
*.crt
*.key
*.pem
*.csr
*.cnf
*.srl

# Public certificates are generated per deployment too; distribute them out of
# band with the matching private material rather than committing host identity.

# Allow this .gitignore and README
!.gitignore
!README.md

# Ignore backup files
*.bak
*.backup
